"""Routers package for the ThinkVelocity AI Unified service.

Exposes the two top-level routers mounted by ``main.py``:
  - ``routers.ai.ai_router``      → mounted at ``/ai``   (prompt-enhance, Server 3)
  - ``routers.context.context_router`` → mounted at ``/context`` (context-engine, Server 2)
"""
